{
  "name": "music-randomizer",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^18.7.6",
    "@types/shuffle-array": "^1.0.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "shuffle-array": "^1.0.1"
  },
  "scripts": {
    "addIndex": "ts-node-dev --transpile-only --ignore-watch node_modules src/addIndex.ts",
    "removeIndex": "ts-node-dev --transpile-only --ignore-watch node_modules src/removeIndex.ts",
    "reloadIndex": "ts-node-dev --transpile-only --ignore-watch node_modules src/reloadIndex.ts"
  }
}
